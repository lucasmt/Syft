#pragma once

#include <iostream>

/*
template<class T>
void report(const std::string& disclaimer, const T& data)
{
  std::cerr << disclaimer << data << std::endl;
}
*/

/*
void print_transition(const BDD& current_minterm,
                      const BDD& condition,
                      const BDD& next_var)
{
  std::cerr << current_minterm << " -> "
            << next_var << " = "
            << condition << std::endl;
}
*/
